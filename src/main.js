import { createApp } from 'vue';
import App from './App.vue';
// import router from './router'; // Make sure your router file is correctly configured

const app = createApp(App);
// app.use(router);

app.mount('#app');
